# syg-app

App is a container element for an Ionic application. There should only be one `<syg-app>` element per project. An app can have many Ionic components including menus, headers, content, and footers. The overlay components get appended to the `<syg-app>` when they are presented.

<!-- Auto Generated Below -->


----------------------------------------------

*Built with [StencilJS](https://stenciljs.com/)*
